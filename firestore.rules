rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Usage counters and source configuration are server-only.
    // Cloud Functions uses Admin SDK and bypasses client rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
